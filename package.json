{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest"
  },
  "devDependencies": {
    "nuxt": "3.0.0",
    "vitest": "^0.25.3"
  },
  "dependencies": {
    "@nuxtjs/tailwindcss": "^6.1.3"
  }
}
